using System;

namespace ConsoleApp2
{
    class Program
    {
        public static void Main(string[] args)
        {
          // Programming_practices programmingPractices = new Programming_practices();
           // programmingPractices.Run();
         //  Programming_practices.Run();
            // string name = "Sam";
            // int roll_no = 1;
            // double percent = 99.99;
            // string result = "Pass";
            //
            // Console.WriteLine($"Displaying Result:\n{name} with Roll Number {roll_no} has Scored {percent}% Marks and Result is {result}");
            //
            // double distanceA = 156.6;
            // double distanceB = 211.8;
            //
            // double totalDistance = distanceA + distanceB;
            //
            // int timeA = (4 * 60) + 4;
            // int timeB = (4 * 60) + 25;
            //
            // int totalTime = timeA + timeB;
            // int totalhours = totalTime / 60;
            // int totalMinutes = totalTime % 60;
            //
            // Console.WriteLine($"Total Distance from chennai to bangalore is {totalDistance}");
            // Console.WriteLine($"Total Time Taken = {totalhours} hours {totalMinutes} minutes");
            
            // Check if number is divisble by 5
            
            // Console.Write("Enter a number: ");
            // int num = int.Parse(Console.ReadLine());
            // int div = num % 5;
            // if (div == 0)
            // {
            //     Console.WriteLine("It is divisble by 5");
            // }
            // else
            // {
            //     Console.WriteLine("It is not divisble by 5");
            // }
            
            // Console.Write("number1, number2, number3");
            // int num1 = int.Parse(Console.ReadLine());
            // int num2 = int.Parse(Console.ReadLine());
            // int num3 = int.Parse(Console.ReadLine());
            //
            // if (num1 < num2 && num1 < num3)
            // {
            //     Console.WriteLine("Number1 is smallest");
            // }
            // else if (num1 > num2 || num1 > num3)
            // {
            //     Console.WriteLine("Number 1 is not smallest");
            // }
            
            //Level 3 if, while, for
            
            // Console.Write("Enter the number: ");
            // int number = int.Parse(Console.ReadLine());
            //
            // int originalNumber = number;
            // int sum = 0;
            // while (originalNumber != 0)
            // {
            //     int remainder = originalNumber % 10;
            //     sum = sum + (remainder * remainder * remainder);
            //     originalNumber = originalNumber / 10;
            // }
            //
            // if (sum == number)
            // {
            //     Console.WriteLine($"{number} is an Armstrong number");
            // }
            // else
            // {
            //     Console.WriteLine($"{number} is not an Armstrong number");
            // }
            
            //Count the number of digits
            
            // // Console.Write("Enter the number: ");
            // int num = int.Parse(Console.ReadLine());
            // int count = 0;
            // while (num != 0)
            // {
            //     num = num / 10;
            //     count++;
            // }
            // Console.WriteLine($"Number of digits: {count}");
            //
            // Console.Write("Enter a number: ");
            // int num = int.Parse(Console.ReadLine());
            // int originalNumber = num;
            // int sum = 0;
            // while (num != 0)
            // {
            //     sum = sum + (num % 10);
            //     num = num / 10;
            // }
            //
            // if (originalNumber % sum == 0)
            // {
            //     Console.WriteLine($"{originalNumber} is a Hardhad number");
            // }
            // else
            // {
            //     Console.WriteLine($"{originalNumber} is not a Harshad Number");
            // }
            


        }
    }
}